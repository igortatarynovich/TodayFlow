# X11 leftover narrative — CLOSED / PASS

**Date:** 2026-09-21  
**Branch:** `cursor/x11-leftover-narrative`  
**Base:** `f9411278` (X11 selection on `cursor/x4-x5-honest-reveal-copy`)  
**Pass bound:** **X11: CLOSED / PASS.** Production `/today` speaks one locked 4-surface Inventory narrative. Leftover parallel surfaces do not mount as product Today. TIC remains **CLOSED / PASS**. N = 20. X3 remains **CLOSED / PASS**. X4/X5 remains **CLOSED / PASS**.

Not two trains. Not TIC-K21. Not landing. Not X14. Do not rebuild server until asked.

---

## 0. Decision (locked)

X11 is **closed**. Product `/today` always mounts `TodayCompositionSurface`. Query params `?full=1` / `?experience=1` are no-ops. Glance-as-act is not constructed on the locked surface. Evening on the 4-surface is gratitude, not promise-trap drama.

Leftover files (`TodayGlanceAct`, `TodayExperienceSurface`, `TodayRitualFlow`, `todayRitualCopy` bank) stay in the repo. Physical delete is out of gate.

Selected next product remainder: Full User Path **X14** (progress + D2 continuity). Closing X11 does not implement or close that gate.

---

## 1. Live audit then paint

| Named leftover | Live before this hop | After |
|----------------|----------------------|-------|
| Glance-as-act | Unmounted (`TodayGlanceAct` only in its test). Dead `glanceSection` still constructed inside `TodayCompositionSurface` | `glanceSection` removed. Locked render has no `today-zone-glance` / `today-zone-glance-act` |
| `?experience=1` / `?full=1` | Production NODE_ENV already no-op (live guest `/today`, `/today?full=1`, `/today?experience=1` = pitch). Same route still imported leftover surfaces for `NODE_ENV === "development"` | Product `/today` no longer imports or mounts `TodayExperienceSurface` / `TodayRitualFlow`. Query params cannot switch the route |
| promise-trap / duplicate `day_story` | Locked evening already gratitude. Stacked promise UI behind dead `useLegacyStackedPath` | Unchanged mechanic. Tests lock: gratitude present; `evening-promise-picker` / `today-zone-promise` absent |
| deferred `todayRitualCopy` honesty | `numberCircleHint` «Интуитивно выбери своё число» unused on locked path | Unreachable on product `/today`. Copy bank not rewritten. Leftover files stay |

Server was not rebuilt. Guest live check: leftover copy (`выбери ту`, `своё число`) absent from `/today` DOM.

---

## 2. What passed

- Same 4-surface order. No new Inventory slot. No new TIC-K. No producer change. No ScreenFlow reorder.
- X3 lens copy and X4/X5 reveal chrome still hold.

Gate: `todayX11LeftoverNarrative.test.tsx` · `todayX11RouteSurfaces.test.ts`. X3 / X4/X5 gates unchanged. TIC close-out unchanged PASS.

---

## 3. Do not

- Physical-delete leftover files from this freeze.
- Start **X14** from this freeze (new branch when that gate starts).
- Invent TIC-K21 or resume TIC hops.
- Rebuild server. Merge/deploy only if asked.
- Reopen Glance as a fifth act. Resume PIC / IL / Compatibility IC.
