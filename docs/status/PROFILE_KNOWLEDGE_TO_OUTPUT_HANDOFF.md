# Profile knowledge-to-output — next agent

**Date:** 2026-09-20  
**Branch:** `cursor/profile-knowledge-to-output`  
**Parked:** P1 library fill on `cursor/p1-self-compassion-loop-notes` @ `f48a438d`  
**Pass bound:** PIC N=18 drives Profile. Occupancy F03/F06 → K01 qualifier closed. K13 COMPLETE. K02 COMPLETE. K12 COMPLETE. **K05 COMPLETE.** Coverage: [PROFILE_INFORMATION_CONTRACT_V1 §11](../profile/PROFILE_INFORMATION_CONTRACT_V1.md).

Not remaining P1. Not IL-3→Stage 1. Not TODAY_INFORMATION_CONTRACT. Do not rebuild server until the PIC queue is worked.

---

## 0. Decision (locked)

PIC управляет разработкой. Сначала `PIC-K*` + исходные `PIC-F*`, потом вся цепочка до slot. Не «что ещё протащить из движка».

K01 thesis может остаться `builds_through_analysis`. Occupancy меняет конкретизацию, не двух разных людей.

**Profile train is not closed.** 9 COMPLETE / 8 PARTIAL / 1 MISSING. Then TODAY_INFORMATION_CONTRACT.

---

## 1. First 15 minutes

1. This file + tracker NOW (`PIC K05 COMPLETE`) + Information Contract §11 queue.
2. Stay on `cursor/profile-knowledge-to-output`. Next code: **K04** — одна ось Internal Engine в `P3` из F07/F08; не семь виджетов. F07 уже в Stage 0 (K05 hop); F08 по-прежнему нет. Не чинить K06.
3. Run: `backend/.venv/bin/pytest tests/test_profile_knowledge_to_output_v1.py tests/test_character_engine_stage01_v0.py tests/test_character_engine_profile_consumption_v0.py tests/test_character_engine_stage3_internal_v0.py -q --tb=short --no-cov`
4. G0 stays deferred. Do not untrip `DATA/ops/llm_spend.json`. Do not compose-rebuild.

---

## 2. Occupancy (closed)

Same Sun Virgo / Moon Taurus / ASC Gemini. Mars Cancer H4 vs Libra H7.

Thesis identical: `builds_through_analysis`. Surface/recognition differs by IL-2 lemmas. Occupancy = `qualifier`. Not IL-3. Not IL-4. Catalog `draft`.

---

## 2b. K13 (closed)

Producer `core_profile` + Matrix bag keys: `expression` · `soul_urge` · `personality`. Consumer `profileHeaderFacts` reads those keys (`*_number` alias only). Nameless: omit slot + K17 `need_name`. No Identity Core / natal write.

---

## 2c. K02 (closed)

CE consumption keeps natal Why rows (`sun` / `moon` / `asc` / `life_path`) from the projector or Stage 0. Occupancy `planet_in_sign` / `planet_in_house` is not a Why row and does not map to `P2.anchor.rhythm`. Identity thesis unchanged.

---

## 2d. K12 (closed)

`P2.selected_life_path` = F09 `life_path` + `number_base_v1.base_meaning`. CE primary claim is not this slot. No birthday mix. No essay fallback: missing contribution omits.

---

## 2e. K05 (closed)

Necessary F07 hop is inside this chain: Stage 0 mints 5-major `aspect_pair:*` (Foundation orbs, Sun–Saturn). Stage 1 composes IL-2 `aspect_pair` and emits **exactly one** hard tension (square / opposition). Consumption writes that A↔B line to `P3.insight`. Trap-bank and Stage3 identity essays cannot beat grounded evidence. No hard F07 → omit, not synthetic tension. Harmonics are facts, not K05. K06 secondaries not wired. K01/K02/K12/K13 unchanged.

---

## 3. Queue (from §11, not architecture leftovers)

1. K04 — one Internal Engine axis into P3 from F07/F08; not seven widgets. F07 exists in Stage 0; engine still reads identity thesis.
2. K09 — fill-empty one blind_spot into insight/help.
3. K07 — ≤2 spheres from F06 + emit.
4. K10 — omit empty helps; no essay fallback.
5. K06 — omit from M or ≤2 into node materials.
6. K15 — emit `P6.natal_decode` / Explore.
7. K16 — Inventory slot or drop from M.
8. K03 — ASC/MC how on P2; house how Explore.

COMPLETE already: K02, K05, K08, K11 (bridge), K12, K13, K14, K17, K18.

---

## 4. Do not

- Resume P1 fill.
- Expand IL (transits, angles, occupancy dump, K06 secondaries).
- Change 13-key K01 thesis because Mars differs.
- Invent PIC-K19.
- Start TODAY_INFORMATION_CONTRACT.
- Rebuild/recreate compose services on this host for this queue.
- Untrip billing. Merge/deploy only if asked.
