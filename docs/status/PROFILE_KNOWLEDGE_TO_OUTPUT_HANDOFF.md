# Profile knowledge-to-output — next agent

**Date:** 2026-09-20  
**Branch:** `cursor/profile-knowledge-to-output`  
**Parked:** P1 library fill on `cursor/p1-self-compassion-loop-notes` @ `f48a438d`  
**Pass bound (closed 2026-09-20):** Mars × sign × house survives Stage 1 occupancy claims, Identity Core surface, and `P1.recognition_line`. Harness: `backend/tests/test_profile_knowledge_to_output_v1.py` (xfail inverted).

Not a new product canon. Not a new SoT. Not a 30-page audit. Not remaining P1 types. Not the rest of IL.

---

## 0. Decision (locked)

P1 Library Fill **STOPPED**. Coverage stays **153 items, 20/42 P1 sourced, 5 P1 skipped**.

Occupancy wire is **closed**. Do not keep attaching more IL frames because another signal was found.

**Next train:** Profile Information Contract — closed list of information Profile may output. Then attach knowledge against that list. Do not start Today / Compatibility / Tarot on this pass.

---

## 1. First 15 minutes

1. This file + tracker NOW (`PROFILE OCCUPANCY PASS`) + `CALC_IL_WIRE_V1` occupancy Architecture impact.
2. Stay on `cursor/profile-knowledge-to-output`. Do not resume P1 fill. Do not wire aspects / transits / angles.
3. Run: `backend/.venv/bin/pytest tests/test_profile_knowledge_to_output_v1.py tests/test_character_engine_stage01_v0.py tests/test_character_engine_stage2_identity_v0.py -q --tb=short --no-cov`
4. G0 stays deferred. Do not untrip `DATA/ops/llm_spend.json`.

---

## 2. Four questions — after occupancy pass (2026-09-20)

Two natal snapshots, **same Sun Virgo / Moon Taurus / ASC Gemini**. Only Mars differs: Cancer house 4 vs Libra house 7.

| Hop | Cancer H4 | Libra H7 |
|-----|-----------|----------|
| Calc / IL-2/3 | distinct occupancy frames | distinct occupancy frames |
| CE Stage 0 | `{sign: cancer, house: 4}` | `{sign: libra, house: 7}` |
| CE Stage 1 | 13-key thesis **plus** `planet_in_sign:mars:cancer` / `planet_in_house:mars:04` | same 13-key **plus** Libra / 07 |
| Identity thesis | `builds_through_analysis` (unchanged 13-key) | identical thesis |
| Identity surface / recognition | 13-template + occupancy lemmas (`close`/`holding`/`home`) | 13-template + (`balancing`/`partnership`) |

Stage 1 still does not import aspects, transits, or angles. Catalog stays `draft`. Display / prompts / new CE concepts were not touched.

---

## 3. What this pass did

1. Stage 1 mints IL-3 `planet_in_sign` / `planet_in_house` as `mechanism` claims from Stage 0 `planet_sign` rows. Omit if compose refuses.
2. Identity thesis stays the 13-key registry. Occupancy lemmas fill-empty onto Identity Core `surface_text` (Stage 5 / consumption copy that into `recognition_line`).
3. Only Stage 1 imports IL. No pair catalog. No `active`. No new claim_kind.

---

## 4. Do not

- Continue P1 types on this pass.
- Wire remaining IL frames (aspects, transits, angles, the rest of occupancy dump-as-product).
- Write a new Profile/CE/IL canon or a 30-page audit.
- Set catalog `active` as a side effect.
- Let the LLM choose Saturn □ Venus / Mars house.
- Invent Russian meaning when IL refuses — omit.
- Touch display, prompts, or new CE concepts.
- Untrip billing. Merge/deploy only if asked.
- Start Today / Compatibility / Tarot until Profile Information Contract names what Profile may output.
