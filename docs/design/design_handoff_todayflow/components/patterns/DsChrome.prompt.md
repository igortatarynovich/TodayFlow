Top-level chrome — the marketing top nav (anchors + CTA pill), the product app's left sidebar (logo, avatar, nav, settings), and the page header row used at the top of any product-app screen.

```jsx
<DsAppSidebar displayName="Igor" avatarInitial="I" navItems={navItems} activeHref="/today" />
```
