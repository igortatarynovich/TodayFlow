Small marketing/insight building blocks used inside sections — feature grids on the landing page, aside rows next to a theme panel, practice list rows.

```jsx
<DsFeatureTile icon={<IconSun/>} title="Morning with direction" body="Open Today and see where to look." />
```
