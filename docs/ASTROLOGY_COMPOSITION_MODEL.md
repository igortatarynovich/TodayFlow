# Astrology Composition Model (ACM-Compose)

**Статус:** принято (канон **gate перед P0.8** — первичные vs производные сущности).  
**Версия:** 1.0 (2026-05-31).  
**Владелец:** Product + Engineering.

**Роль:** запретить раздувание Reference Layer композиционными записями (120+ planet-sign, сотни aspect-pair). **P0.8** — только **атомы**; композиция — **фаза 2 Rules** (Astrology Composition Engine).

**Связь:** [ASTROLOGY_MACHINE_CONTRACT.md](./ASTROLOGY_MACHINE_CONTRACT.md), [ONTOLOGY_AND_FOUNDATION_PHASES.md](./ONTOLOGY_AND_FOUNDATION_PHASES.md) (фаза 1 vs 2), [CROSS_DOMAIN_MACHINE_VALIDATION.md](./CROSS_DOMAIN_MACHINE_VALIDATION.md) (P0.9 compose at validation, not in CD), [DATA_ORIGINATION_AND_LIFECYCLE.md](./DATA_ORIGINATION_AND_LIFECYCLE.md).

---

## 0. Главный вопрос

> **Какие астрологические сущности первичные (Reference), а какие производные (Derived)?**

| Класс | Где живёт | Creation method |
|-------|-----------|-----------------|
| **Primary (atomic)** | `DATA/reference/astrology/machine/` | Manual editorial (P0.8) |
| **Composite (derived)** | runtime / SN — **не** JSON catalog | Generated by **Astrology Composition Engine** (фаза 2) |

---

## 1. Первичные сущности (Reference Layer — P0.8)

**Freeze P0.8:** ровно **39** machine records. Только атомы.

| entity_type | Count | entity_code | Example file |
|-------------|-------|-------------|--------------|
| **ZodiacSign** | 12 | `astrology.sign.{id}` | `sign_aries.json` |
| **Planet** | 10 | `astrology.planet.{id}` | `planet_mars.json` |
| **House** | 12 | `astrology.house.{nn}` | `house_01.json` |
| **Aspect** | 5 | `astrology.aspect.{id}` | `aspect_square.json` |

Major aspects only: `conjunction`, `sextile`, `square`, `trine`, `opposition`.

**Итого: 39.** CI gate enforces exact set.

---

## 2. Производные сущности (НЕ Reference — фаза 2+)

| Composite | Пример | Появляется через | Запрещено в P0.8 |
|-----------|--------|------------------|------------------|
| **PlanetInSign** | Mars in Aries | Compose(`planet.mars`, `sign.aries`) | ❌ `planet_in_sign.*` JSON |
| **PlanetInHouse** | Sun in House 10 | Compose(`planet.sun`, `house.10`) | ❌ |
| **AspectPair** | Mars square Saturn | Compose(`aspect.square`, bodies, orbs) | ❌ |
| **TransitSignal** | Moon square Saturn today | Daily Engine + ephemeris | ❌ |
| **DailySpine** | foundation.spine | Daily Engine | ❌ |

**Anti-pattern (запрет):**

- 120 planet-sign machine files  
- 120 planet-house machine files  
- сотни pre-baked aspect-pair records  

Legacy `planet_in_sign_relationships.json` — **content only**; не Machine Contract catalog.

**Interpretation Library Layer 5** ([INTERPRETATION_LIBRARY_V1](astrology/INTERPRETATION_LIBRARY_V1.md)): curated semantic objects (`DATA/reference/astrology/interpretation_v1/`) allowed **only** where composition of atoms is unreliable (`curation_reason: non_compositional`). IL-1 gold list = **candidates**; IL-2 may demote to composed. Это не machine JSON и не каталог всех пар. ACM freeze на `DATA/reference/astrology/machine/` composite files **не снимается**.

---

## 3. Astrology Composition Engine (фаза 2 — backlog)

**Не P0.8.** Реализуется после P0.9 / перед или вместе с P1.0.

```
Inputs:  atomic machine records (CD)
Rules:   composition weights, orb policy, dignity modifiers (TBD)
Output:  DerivedAstroSignal (DD) — same DayModel-facing shape as §5.3 DAYMODEL
```

### 3.1 Sketch: PlanetInSign (example)

```
vector = normalize(w_p * V_planet + w_s * V_sign)     # default w_p=0.55, w_s=0.45
tempo  = merge_tempo(planet.tempo, sign.tempo)
risk_modifier = clamp(planet.risk_modifier + 0.5 * sign.risk_modifier)
confidence = min(planet.confidence, sign.confidence) * composition_discount
```

Exact weights for **machine vectors** — **Rules doc** + calibration in **P0.9** (anchor: Chariot / Year 8 / **mars + aries atoms**, not a composite file).

Lemma composition (what/how/where/relation/orientation) is a **different layer**: [IL2_COMPOSITION_RULES_V1.md](astrology/IL2_COMPOSITION_RULES_V1.md) (1.3.107). Theme rank of those frames is [IL3_INTERPRETATION_ENGINE_V1.md](astrology/IL3_INTERPRETATION_ENGINE_V1.md) (1.3.108) — sky-internal, not user relevance. Voice of ranked themes is [IL4_EXPRESSION_V1.md](astrology/IL4_EXPRESSION_V1.md) (1.3.109) — not meaning. Coverage of locked engines is [LIBRARY_SCALE_V1.md](astrology/LIBRARY_SCALE_V1.md) (1.3.110) — not a pair catalog. Role weights 0.55/0.45 are **not** lemma-string averaging. ACM composite **machine** JSON freeze stands.

### 3.2 Sketch: AspectPair

```
vector = aspect_vector modulated by planet weights
risk   = f(aspect.risk, aspect.risk_modifier, tension from legacy orb)
```

---

## 4. P0.8 deliverables (atomic only)

1. **39** machine drafts in `DATA/reference/astrology/machine/`  
2. `load_astrology_machine_contracts()` + domain lookup  
3. Validator: schema + count + entity_code set + **forbidden composite patterns**  
4. Tests: load count, enums, no composite codes  
5. **No** Composition Engine code  
6. **No** UI, generation, DayModel wiring  

---

## 5. P0.9 cross-domain (compose at validation time)

Anchor «Mars in Aries» для CDMV:

| Piece | Source |
|-------|--------|
| Chariot | `tarot.major.07` (CD) |
| Personal Year 8 | `numerology.personal_year.8` (CD) |
| Mars + Aries | `astrology.planet.mars` + `astrology.sign.aries` — **compose in test** per §3.1 sketch |

Не создавать `astrology.planet_in_sign.mars.aries` в Reference.

---

## 6. CI forbidden patterns (P0.8+)

Validator **rejects** `entity_code` matching:

- `astrology.planet_in_sign.`
- `astrology.planet_in_house.`
- `astrology.transit.`
- `astrology.aspect_pair.`
- `astrology.foundation.`

Allowed prefix roots only: `astrology.sign.`, `astrology.planet.`, `astrology.house.`, `astrology.aspect.`

---

## 7. Checklist

- [x] Primary vs derived taxonomy (this doc)  
- [x] P0.8 — 39 atomic drafts + loader + validator  
- [ ] P0.9 — cross-domain with atomic compose  
- [x] Composition Engine **lemma** rules (IL-2 / 1.3.107) — [IL2_COMPOSITION_RULES_V1.md](astrology/IL2_COMPOSITION_RULES_V1.md). Machine-vector calibration still P0.9.  
- [x] Interpretation Engine **theme rank** (IL-3 / 1.3.108) — [IL3_INTERPRETATION_ENGINE_V1.md](astrology/IL3_INTERPRETATION_ENGINE_V1.md). Sky-internal. Not user relevance.  
- [x] Expression **voice packs** (IL-4 / 1.3.109) — [IL4_EXPRESSION_V1.md](astrology/IL4_EXPRESSION_V1.md). Not meaning. Not LLM choosing Saturn □ Venus.  
- [x] Library **scale** (1.3.110) — [LIBRARY_SCALE_V1.md](astrology/LIBRARY_SCALE_V1.md). Coverage contract; not a pair catalog. Wire named, then live at library layer 1.3.111.
- [x] Calc → IL **wire** (1.3.111) — [CALC_IL_WIRE_V1.md](astrology/CALC_IL_WIRE_V1.md). Library layer. Product surfaces not attached.  
- [x] Interpretation Library atoms (IL-1) then curated Layer 5 — [INTERPRETATION_LIBRARY_V1.md](astrology/INTERPRETATION_LIBRARY_V1.md); IL-2 demoted gold-with-atoms to composed  
- [ ] P1.0 — DayModel astro slice from composed daily signals  

---

## 8. Changelog

- **1.0.7 (2026-08-23)** — Calc → IL wire (1.3.111). Pointer to CALC_IL_WIRE_V1. Library layer, not product surfaces. Machine composite freeze unchanged.
- **1.0.6 (2026-08-23)** — Library scale (1.3.110). Pointer to LIBRARY_SCALE_V1. Coverage contract, not pair catalog. Machine composite freeze unchanged.
- **1.0.5 (2026-08-23)** — IL-4 Expression (1.3.109). Pointer to IL4_EXPRESSION_V1. Voice, not meaning. Machine composite freeze unchanged.
- **1.0.4 (2026-08-23)** — IL-3 Interpretation Engine (1.3.108). Pointer to IL3_INTERPRETATION_ENGINE_V1. Sky-internal theme rank. Machine composite freeze unchanged.
- **1.0.3 (2026-08-23)** — IL-2 lemma composition rules (1.3.107). Pointer to IL2_COMPOSITION_RULES_V1. Machine composite freeze unchanged.
- **1.0.2 (2026-08-17)** — IL-1 Layer 5 gold list = curated candidates; IL-2 may demote to composed (IL activation gate; no ACM freeze change). **Done 1.3.107.**
- **1.0.1 (2026-08-17)** — Layer 5 interpretation objects ≠ machine composites; pointer to Interpretation Library.
- **1.0 (2026-05-31)** — atomic-only P0.8 freeze; composite → Composition Engine; CDMV anchor via mars+aries atoms.

---

*Любой PR с composite astrology machine JSON в `DATA/reference/astrology/machine/` — reject до снятия freeze.*
