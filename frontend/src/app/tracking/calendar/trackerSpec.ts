/**
 * Уровень инсайтов трекера (FREE / PRO / PREMIUM).
 * Копирайт UI — `flowPracticesMainTabChrome.ts` / `FlowTrackerChromeCopy`.
 */
export type TrackerTier = "free" | "pro" | "premium";

export const DEFAULT_TRACKER_TIER: TrackerTier = "free";
