"use client";

import { NumberIcon, type NumberIconProps } from "@/components/visualIdentity/NumberIcon";

/** DS-only number glyph entrypoint (Form Kit visual contract). */
export function DsNumber(props: NumberIconProps) {
  return <NumberIcon {...props} />;
}
