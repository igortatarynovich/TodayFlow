# Zodiac seal icons (gold)

Metallic circular zodiac glyphs for `ZodiacIcon` (all call sites).

```text
{slug}.webp   # 320×320, transparent
{slug}.svg    # legacy line glyph (tintable mask / tooling)
```

Source sheet: `docs/design/assets/zodiac-glyphs-sheet.png`  
Rebuild: `python3 scripts/crop_celestial_icon_sheets.py`

Painterly portraits (separate) live in `../../zodiac/` via `ZodiacIllustration`.
