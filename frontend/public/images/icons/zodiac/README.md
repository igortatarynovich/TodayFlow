# Zodiac seal icons (gold)

3D metallic silver/gold glyphs for `ZodiacIcon` (Today planet+sign and other call sites).

```text
{slug}.webp   # 320×320, transparent
{slug}.svg    # legacy line glyph (tintable mask / tooling)
```

Source sheet: `docs/design/assets/zodiac-metal-glyphs-sheet.png`  
Rebuild: `python3 scripts/crop_celestial_icon_sheets.py --zodiac-only`

Painterly portraits (separate) live in `../../zodiac/` via `ZodiacIllustration`.
